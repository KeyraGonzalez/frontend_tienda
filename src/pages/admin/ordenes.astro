---
import Layout from '../../layouts/Layout.astro';
---

<Layout 
  title="Gestión de Órdenes - StyleHub Admin"
  description="Panel de administración para gestionar órdenes de StyleHub."
>
  
  <main class="min-h-screen bg-white">
    <!-- Loading State -->
    <div id="loading" class="flex items-center justify-center min-h-screen">
      <div class="text-center">
        <div class="inline-block animate-spin rounded-full h-8 w-8 border-b-2 border-black"></div>
        <p class="mt-2 text-black font-medium tracking-wide">Cargando panel de órdenes...</p>
      </div>
    </div>

    <!-- Access Denied -->
    <div id="access-denied" class="hidden flex items-center justify-center min-h-screen">
      <div class="text-center max-w-md mx-auto px-4">
        <div class="w-16 h-16 bg-white border-2 border-black rounded-full flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-black" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z" />
          </svg>
        </div>
        <h1 class="text-2xl font-bold text-black mb-2 tracking-wide uppercase">Acceso Denegado</h1>
        <p class="text-gray-600 mb-6">No tienes permisos de administrador para acceder a esta página.</p>
        <a href="/" class="bg-black text-white px-6 py-2 rounded-lg font-bold hover:bg-gray-800 transition-colors tracking-wide uppercase">
          Volver al inicio
        </a>
      </div>
    </div>

    <!-- Orders Panel -->
    <div id="orders-panel" class="hidden">
      <!-- Header -->
      <div class="bg-black text-white border-b-2 border-black">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex items-center justify-between h-20">
            <div class="flex items-center">
              <a href="/admin" class="flex items-center text-white hover:text-gray-300 mr-6">
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18" />
                </svg>
                Panel Principal
              </a>
              <h1 class="text-2xl font-bold tracking-wide uppercase">Gestión de Órdenes</h1>
            </div>
            <div class="flex items-center space-x-4">
              <button id="export-orders-btn" class="bg-white text-black px-4 py-2 rounded-lg font-bold hover:bg-gray-100 transition-colors tracking-wide uppercase">
                <svg class="w-5 h-5 inline mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
                </svg>
                Exportar
              </button>
            </div>
          </div>
        </div>
      </div>

      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        
        <!-- Statistics Cards -->
        <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
          <div class="bg-white rounded-lg shadow-sm p-6 border-2 border-black">
            <div class="flex items-center">
              <div class="w-12 h-12 bg-black rounded-lg flex items-center justify-center mr-4">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                </svg>
              </div>
              <div>
                <p class="text-sm font-bold text-black tracking-wide uppercase">Órdenes Hoy</p>
                <p class="text-2xl font-bold text-black" id="orders-today">0</p>
              </div>
            </div>
          </div>

          <div class="bg-white rounded-lg shadow-sm p-6 border-2 border-black">
            <div class="flex items-center">
              <div class="w-12 h-12 bg-black rounded-lg flex items-center justify-center mr-4">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                </svg>
              </div>
              <div>
                <p class="text-sm font-bold text-black tracking-wide uppercase">Órdenes Mes</p>
                <p class="text-2xl font-bold text-black" id="orders-month">0</p>
              </div>
            </div>
          </div>

          <div class="bg-white rounded-lg shadow-sm p-6 border-2 border-black">
            <div class="flex items-center">
              <div class="w-12 h-12 bg-black rounded-lg flex items-center justify-center mr-4">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
                </svg>
              </div>
              <div>
                <p class="text-sm font-bold text-black tracking-wide uppercase">Pendientes</p>
                <p class="text-2xl font-bold text-black" id="orders-pending">0</p>
              </div>
            </div>
          </div>

          <div class="bg-white rounded-lg shadow-sm p-6 border-2 border-black">
            <div class="flex items-center">
              <div class="w-12 h-12 bg-black rounded-lg flex items-center justify-center mr-4">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1" />
                </svg>
              </div>
              <div>
                <p class="text-sm font-bold text-black tracking-wide uppercase">Ingresos</p>
                <p class="text-2xl font-bold text-black" id="orders-revenue">$0</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Filters -->
        <div class="bg-white rounded-lg shadow-sm border-2 border-black p-4 mb-6">
          <div class="grid grid-cols-1 md:grid-cols-5 gap-4">
            <div>
              <label class="block text-sm font-bold text-black mb-1 tracking-wide uppercase">Buscar</label>
              <input type="text" id="orders-search" placeholder="ID de orden, cliente..." 
                     class="w-full px-3 py-2 border-2 border-black rounded-lg focus:outline-none focus:ring-2 focus:ring-black">
            </div>
            <div>
              <label class="block text-sm font-bold text-black mb-1 tracking-wide uppercase">Estado</label>
              <select id="orders-status" class="w-full px-3 py-2 border-2 border-black rounded-lg focus:outline-none focus:ring-2 focus:ring-black">
                <option value="">Todos</option>
                <option value="pending">Pendiente</option>
                <option value="processing">Procesando</option>
                <option value="confirmed">Confirmado</option>
                <option value="shipped">Enviado</option>
                <option value="delivered">Entregado</option>
                <option value="cancelled">Cancelado</option>
              </select>
            </div>
            <div>
              <label class="block text-sm font-bold text-black mb-1 tracking-wide uppercase">Fecha desde</label>
              <input type="date" id="orders-date-from" 
                     class="w-full px-3 py-2 border-2 border-black rounded-lg focus:outline-none focus:ring-2 focus:ring-black">
            </div>
            <div>
              <label class="block text-sm font-bold text-black mb-1 tracking-wide uppercase">Fecha hasta</label>
              <input type="date" id="orders-date-to" 
                     class="w-full px-3 py-2 border-2 border-black rounded-lg focus:outline-none focus:ring-2 focus:ring-black">
            </div>
            <div class="flex items-end space-x-2">
              <button id="orders-filter-btn" class="flex-1 bg-black text-white px-4 py-2 rounded-lg hover:bg-gray-800 transition-colors font-bold tracking-wide uppercase">
                Filtrar
              </button>
              <button id="clear-filters-btn" class="bg-gray-200 text-black px-4 py-2 rounded-lg hover:bg-gray-300 transition-colors font-bold tracking-wide uppercase">
                Limpiar
              </button>
            </div>
          </div>
        </div>

        <!-- Orders Table -->
        <div class="bg-white rounded-lg shadow-sm border-2 border-black overflow-hidden">
          <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-black">
              <thead class="bg-black">
                <tr>
                  <th class="px-6 py-3 text-left text-xs font-bold text-white uppercase tracking-wider">ID / Fecha</th>
                  <th class="px-6 py-3 text-left text-xs font-bold text-white uppercase tracking-wider">Cliente</th>
                  <th class="px-6 py-3 text-left text-xs font-bold text-white uppercase tracking-wider">Total</th>
                  <th class="px-6 py-3 text-left text-xs font-bold text-white uppercase tracking-wider">Estado</th>
                  <th class="px-6 py-3 text-left text-xs font-bold text-white uppercase tracking-wider">Ubicación</th>
                  <th class="px-6 py-3 text-left text-xs font-bold text-white uppercase tracking-wider">Acciones</th>
                </tr>
              </thead>
              <tbody id="orders-table-body" class="bg-white divide-y divide-gray-200">
                <tr>
                  <td colspan="6" class="px-6 py-8 text-center text-black">
                    <div class="flex flex-col items-center">
                      <svg class="w-12 h-12 mb-4 text-black" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 11V7a4 4 0 00-8 0v4M5 9h14l1 12H4L5 9z" />
                      </svg>
                      <p class="font-medium tracking-wide uppercase">Cargando órdenes...</p>
                    </div>
                  </td>
                </tr>
              </tbody>
            </table>
          </div>
          
          <!-- Pagination -->
          <div class="bg-white px-6 py-4 border-t border-black">
            <div id="orders-pagination" class="flex items-center justify-between">
              <div class="text-sm text-black font-medium">Cargando...</div>
              <div class="flex space-x-2">
                <!-- Pagination buttons will be inserted here -->
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

</Layout>

<script>
  import { initializeOrdenesPage } from '../../scripts/admin/ordenes';

  document.addEventListener('DOMContentLoaded', async function() {
    await initializeOrdenesPage();
  });
</script>
