---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
---

<Layout title="Pago Exitoso - Gracias por tu compra">
  <Header />
  <main class="min-h-screen bg-white pt-24 pb-12">
    <div class="max-w-2xl mx-auto px-4">
      <!-- Success Icon -->
      <div class="text-center mb-8">
        <div class="mx-auto w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mb-4">
          <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
          </svg>
        </div>
        <h1 class="text-3xl font-bold text-black mb-2 tracking-wide uppercase">¡Pago Exitoso!</h1>
        <p class="text-gray-600 font-medium">Gracias por tu compra. Tu pedido ha sido procesado correctamente.</p>
      </div>

      <!-- Order Details -->
      <div class="bg-gray-50 rounded-lg border-2 border-gray-200 p-6 mb-6">
        <h2 class="text-xl font-bold text-black mb-4 tracking-wide uppercase">Detalles del Pedido</h2>
        
        <div class="grid grid-cols-2 gap-4 mb-6">
          <div>
            <p class="text-sm text-gray-600 font-medium">Número de Pedido</p>
            <p class="font-bold text-black" id="order-number">Cargando...</p>
          </div>
          <div>
            <p class="text-sm text-gray-600 font-medium">Fecha</p>
            <p class="font-bold text-black" id="order-date">Cargando...</p>
          </div>
          <div>
            <p class="text-sm text-gray-600 font-medium">Estado</p>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-bold bg-green-100 text-green-800 tracking-wide uppercase" id="order-status">
              Pendiente
            </span>
          </div>
          <div>
            <p class="text-sm text-gray-600 font-medium">Total</p>
            <p class="font-bold text-lg text-black" id="order-total">$0.00</p>
          </div>
        </div>

        <!-- Order Items -->
        <div class="border-t border-gray-300 pt-4">
          <h3 class="font-bold text-black mb-3 tracking-wide uppercase">Productos</h3>
          <div id="order-items" class="space-y-3">
            <!-- Order items will be loaded here -->
          </div>
        </div>
      </div>

      <!-- Shipping Information -->
      <div class="bg-gray-50 rounded-lg border-2 border-gray-200 p-6 mb-6">
        <h2 class="text-xl font-bold text-black mb-4 tracking-wide uppercase">Información de Envío</h2>
        <div id="shipping-info">
          <!-- Shipping details will be loaded here -->
        </div>
      </div>

      <!-- Payment Information -->
      <div class="bg-gray-50 rounded-lg border-2 border-gray-200 p-6 mb-6">
        <h2 class="text-xl font-bold text-black mb-4 tracking-wide uppercase">Información de Pago</h2>
        <div id="payment-info">
          <!-- Payment details will be loaded here -->
        </div>
      </div>

      <!-- Next Steps -->
      <div class="bg-black rounded-lg p-6 mb-6">
        <h2 class="text-lg font-bold text-white mb-2 tracking-wide uppercase">¿Qué sigue?</h2>
        <ul class="text-sm text-gray-300 space-y-2 font-medium">
          <li class="flex items-start">
            <svg class="w-4 h-4 text-white mt-0.5 mr-2 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span>Recibirás un email de confirmación con los detalles de tu pedido</span>
          </li>
          <li class="flex items-start">
            <svg class="w-4 h-4 text-white mt-0.5 mr-2 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span>Procesaremos tu pedido en 1-2 días hábiles</span>
          </li>
          <li class="flex items-start">
            <svg class="w-4 h-4 text-white mt-0.5 mr-2 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
            </svg>
            <span>Te enviaremos información de seguimiento cuando el pedido sea enviado</span>
          </li>
        </ul>
      </div>

      <!-- Action Button -->
      <div class="flex justify-center">
        <button id="continue-shopping-btn" 
                class="w-full sm:w-auto bg-black text-white py-3 px-8 rounded-lg font-bold hover:bg-gray-800 transition-colors tracking-wide uppercase">
          Continuar Comprando
        </button>
      </div>
    </div>
  </main>

  <Footer />
</Layout>

<script>
  import { initializeCheckoutSuccessPage } from '../../scripts/checkout-success.ts';

  document.addEventListener('DOMContentLoaded', async function() {
    await initializeCheckoutSuccessPage();
  });
</script>
